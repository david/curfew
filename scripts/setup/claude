#!/bin/bash
set -euo pipefail

MARKER="$HOME/.local/state/claude-configured"

if [ -f "$MARKER" ]; then
    echo "claude: already configured"
    exit 0
fi

claude auth login
mkdir -p "$(dirname "$MARKER")"
touch "$MARKER"
